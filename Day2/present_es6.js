/*
class Present {
		constructor (len, width, height) {
			this.len = len;
			this.width = width;
			this.height = height;
		}
		get areaOfSmallestSide {
			var sides = [this.len, this.width, this.height];
			sides = sides.sort();
			return sides[0] * sides[1];
		}
		get surfaceArea {
			return 2 *this.len * this.width + 2 * this.width * this.height + 2 * this.height * this.len;
		}
}
*/